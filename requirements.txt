sounddevice>=0.4.6
PyAudioWPatch>=0.2.12
faster-whisper>=1.0.0
nvidia-cublas-cu12
nvidia-cudnn-cu12
# Hardware detection — lets hardware.py's NVML path see the GPU even when
# torch is installed CPU-only.  Tiny (<1 MB), no native deps at runtime.
nvidia-ml-py>=12.0
numpy>=1.24.0
pystray>=0.19.0
Pillow>=10.0.0
soundfile>=0.12.0
soxr>=0.3.0
# resemblyzer is installed separately in setup.bat (needs webrtcvad-wheels instead of webrtcvad)
speechbrain>=1.0.0,<1.1.0
huggingface_hub>=0.25.0,<1.0.0
scikit-learn>=1.3.0
